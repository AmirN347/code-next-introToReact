import { render } from './util';

// TODO: Declare a variable, assign it a number, and return it.
render('primitives1', () => {
  let beverageNumber = 3;
  return beverage;
});

// TODO: Declare a variable, assign it a boolean, and return it.
render('primitives2', () => {
  let openForWork = true;
  return openForWork;
});

// TODO: Declare a variable, assign it to null, and return it.
render('primitives3', () => {
  let pringles = null;
  return pringles;
});
